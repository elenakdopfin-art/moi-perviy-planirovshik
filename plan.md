# План реализации проекта "План на день"

План составлен по `tz.md`. История разработки ведется прямо в этом файле. Проект должен оставаться в рамках трех файлов приложения: `index.html`, `style.css`, `script.js`.

## Шаг 1. Каркас приложения, структура данных и стартовое состояние — выполнен

### Что сделать

- Создать базовую HTML-структуру приложения.
- Подключить `style.css` и `script.js`.
- Сделать верхнюю панель: название, текущий период, кнопки назад/вперед, кнопка "Сегодня", выбор даты, переключатель режимов день/неделя/месяц/год.
- Подготовить место для создания, редактирования и переноса задач.
- Реализовать фиксированные категории, приоритеты и варианты повторения.
- Реализовать чтение и запись данных в `localStorage` по ключу `dayPlanAppData`.
- При первом открытии добавить тестовые данные с датами относительно текущей даты пользователя.
- При повторном открытии не перезаписывать сохраненные данные.

### Готовый результат

Открывается страница "План на день", по умолчанию выбран режим "Неделя" и сегодняшняя дата. В `localStorage` создается корректная структура данных, а тестовые задачи появляются только при первом запуске.

### Автопроверка

- Проверить, что в проекте есть только нужные файлы приложения: `index.html`, `style.css`, `script.js`.
- Проверить, что страница открывается без ошибок в консоли.
- Проверить, что `localStorage.dayPlanAppData` создается при первом запуске.
- Проверить, что структура содержит `version`, `demoSeeded`, `tasks`.
- Проверить, что тестовые данные не дублируются после перезагрузки.
- Проверить, что стартовый режим — "Неделя", стартовая дата — текущая дата браузера.

### Ручная проверка

- Оценить, понятно ли выглядит стартовый экран.
- Оценить, удобно ли расположены верхняя навигация, основная область календаря и кнопка добавления задачи на ноутбуке.

### Что сделано по факту

- Созданы файлы приложения `index.html`, `style.css`, `script.js`.
- В `index.html` добавлены верхняя панель, название "План на день", текущий период, кнопки назад/вперед, кнопка "Сегодня", выбор даты, переключатель режимов, область календаря и блок будущей формы задачи.
- В `style.css` добавлен базовый уютный лиловый стиль, сетка ноутбука и простая адаптация для экранов меньше `1024px`.
- В `script.js` добавлены фиксированные категории, приоритеты, варианты повторения, генерация времени с шагом 15 минут, стартовое состояние "Неделя" и сегодняшняя дата.
- Реализованы чтение и запись `localStorage` по ключу `dayPlanAppData`.
- При первом запуске создаются 6 тестовых записей с датами относительно текущей даты пользователя.
- При повторном чтении данные не перезаписываются и тестовые записи не дублируются.

### Добавления сверх плана

- В `script.js` открыт объект `window.PlanDayApp` с чистыми функциями и справочниками, чтобы следующие шаги и автопроверки могли проверять логику без ручного клика по интерфейсу.
- Кнопки "Сохранить" и "Отмена" в форме задачи пока отключены: создание и действия с задачами запланированы на следующие шаги.
- Кнопки назад/вперед уже есть в интерфейсе, но их логика остается для шага 2, как и полноценная отрисовка календарных режимов.

### Автопроверка шага

- Проверен синтаксис `script.js` через встроенный Node.js.
- Проверена логика `localStorage`: создаются `version`, `demoSeeded`, `tasks`.
- Проверено, что тестовые данные не дублируются при повторном чтении.
- Проверено, что тестовые даты динамические: есть задачи на сегодня, завтра и день рождения примерно через месяц.
- Проверено, что день рождения создается как задача категории `birthday` с ежегодным повтором.
- Проверено, что список времени содержит 96 значений с шагом 15 минут.
- Проверено, что страница открывается в Chrome без ошибок консоли.
- Проверено в браузере, что активен режим "Неделя", отображается "План на день", заполнены категории, приоритеты, повторы и время.

### Что осталось только для ручной проверки

- Оценить, приятно ли выглядит стартовый экран.
- Оценить, удобно ли на ноутбуке расположены верхняя навигация, область календаря и кнопка добавления задачи.

## Шаг 2. Отображение календаря и навигация по периодам — выполнен

### Что сделать

- Реализовать режим "День" с блоком задач без времени вверху и компактным списком только занятых временных интервалов.
- Реализовать режим "Неделя" в виде 7 колонок, начиная с понедельника.
- Реализовать режим "Месяц" классической сеткой с днями предыдущего и следующего месяца.
- Реализовать режим "Год" как компактный обзор 12 месяцев с количеством задач и маленькими отметками категорий.
- Реализовать переключение режимов без потери активной даты.
- Реализовать кнопки назад, вперед, "Сегодня" и выбор даты.
- Отображать задачи по категориям, приоритетам и признаку дня рождения.

### Готовый результат

Пользователь может просматривать день, неделю, месяц и год, переходить между периодами и видеть тестовые задачи в правильных местах календаря.

### Автопроверка

- Проверить переход назад/вперед в каждом режиме: день на 1 день, неделя на 1 неделю, месяц на 1 месяц, год на 1 год.
- Проверить, что кнопка "Сегодня" возвращает текущую дату.
- Проверить, что неделя начинается с понедельника.
- Проверить, что дневной вид показывает только занятые временные интервалы, без пустых часовых строк.
- Проверить, что задачи без времени отображаются вверху дня.
- Проверить, что задачи с временем отображаются отдельными плашками с диапазоном времени.
- Проверить, что месячная сетка содержит дни соседних месяцев, если они нужны для заполнения недель.
- Проверить, что годовой режим не строит 365 отдельных дневных ячеек.

### Ручная проверка

- Оценить читаемость дневного расписания на ноутбуке.
- Оценить, удобно ли сканировать неделю, месяц и год.
- Оценить, не выглядят ли карточки задач пестро из-за сочетания категорий и приоритетов.

### Что сделано по факту

- Реализован режим "День": блок задач без времени сверху и компактные плашки только для занятых временных интервалов.
- Реализован режим "Неделя": 7 колонок, неделя начинается с понедельника.
- Реализован режим "Месяц": классическая сетка с днями предыдущего и следующего месяца для заполнения недель.
- Реализован режим "Год": 12 компактных блоков месяцев, количество записей, маленькие цветные отметки категорий и отметка дней рождения.
- Реализовано переключение режимов без потери активной даты.
- Реализованы кнопки назад, вперед, "Сегодня" и выбор даты через календарное поле.
- Тестовые задачи отображаются в календаре: задачи без времени сверху дня, задачи с временем отдельными плашками с диапазоном, задачи в неделе/месяце/году.
- Карточки задач получают цвет категории, маленький индикатор приоритета, метку времени, метку повтора и отдельную подпись "ДР" для дней рождения.

### Добавления сверх плана

- Добавлена предварительная функция вычисления повторяющихся задач для отображения в видимых периодах. Полная логика повторов, конфликтов, фильтрации и редактирования остается для следующих шагов.
- Добавлены экспортируемые через `window.PlanDayApp` функции дат и повторений, чтобы автопроверки могли проверять границы недели/месяца и появления задач без кликов по интерфейсу.
- Добавлены адаптивные стили для новых календарных сеток: неделя и месяц могут прокручиваться по горизонтали на узких экранах, годовой вид перестраивается.
- После ручной проверки пользователь попросил убрать полную пустую временную сетку из дневного вида. Принято: день теперь показывает только занятые интервалы, например `10:00-11:00`.
- После ручной проверки пользователь попросил убрать постоянную правую панель. Принято: на основной панели добавлена кнопка `+ Добавить новую задачу`, форма скрыта и раскрывается по кнопке.
- После ручной проверки пользователь попросил заменить текстовые обозначения категорий на иконки. Принято: работа — компьютер, дети — ребенок, день рождения — подарок, дом — домик, здоровье — красный плюс, личное — отдых.
- После ручной проверки пользователь попросил сделать стартовым экраном неделю, а в день переходить из недельного вида. Принято: стартовый режим изменен на `Неделя`, заголовок дня недели открывает дневной вид.
- После ручной проверки пользователь попросил заменить иконку дома на более понятную. Принято: используется иконка `🏠`.
- После выполнения шага 5 пользователь уточнил сквозную навигацию календаря. Принято: в режиме `Месяц` клик по ячейке дня открывает план этого дня, в режиме `Год` клик по месяцу открывает режим `Месяц`, откуда можно провалиться дальше в день.

### Автопроверка шага

- Проверен синтаксис `script.js` через встроенный Node.js.
- Проверено, что нет внешних подключений, импортов и библиотек.
- Проверены границы недели: для `2026-08-19` неделя начинается `2026-08-17` и заканчивается `2026-08-23`.
- Проверены границы месячной сетки: для августа 2026 сетка начинается `2026-07-27` и заканчивается `2026-09-06`.
- Проверено вычисление задач для периода: задача без времени попадает в верхний блок дня, задача с временем попадает в нужный день.
- Проверено ежегодное появление дня рождения.
- В Chrome проверено, что стартовый режим "Неделя", активная дата `2026-08-19`, а дневной вид после перехода не содержит пустых часовых рядов.
- В Chrome проверено, что задача "Купить продукты для дома" находится в верхнем блоке дня, а "Рабочая встреча" находится в плашке `10:00-11:00`.
- В Chrome проверено, что недельный вид содержит 7 колонок и начинается с понедельника `2026-08-17`.
- В Chrome проверено, что кнопки назад/вперед работают для дня, недели, месяца и года.
- В Chrome проверено, что кнопка "Сегодня" возвращает текущую дату.
- В Chrome проверено, что месячная сетка содержит 42 ячейки и дни соседних месяцев.
- В Chrome проверено, что годовой вид содержит 12 месяцев, не строит дневные ячейки и показывает цветные отметки категорий.
- В Chrome проверено, что старой боковой панели нет, кнопка `+ Добавить новую задачу` есть и раскрывает форму.
- В Chrome проверено, что текстового дубля `Дом Дом` нет.
- В Chrome проверено, что из режима `Месяц` можно открыть план дня кликом по ячейке даты.
- В Chrome проверено, что из режима `Год` можно открыть выбранный месяц, а из него открыть план дня.
- В Chrome проверено, что переходы `Год -> Месяц` и `Месяц -> День` работают не только кликом, но и клавишей `Enter` по выбранной карточке.

### Что осталось только для ручной проверки

- Оценить читаемость дневного расписания на ноутбуке.
- Оценить, удобно ли сканировать неделю, месяц и год.
- Оценить, не выглядят ли карточки задач пестро из-за сочетания категорий и приоритетов.

## Шаг 3. Создание задач, дней рождения, фильтры и валидация — выполнен

### Что сделать

- Реализовать создание задачи через кнопку `+ Добавить новую задачу` на основной панели и раскрывающуюся форму.
- Реализовать поля: название, категория, дата, "Без времени", время начала, время окончания, приоритет, повторение, заметка.
- Сделать обязательными название, категорию и дату.
- Для задач с временем требовать время начала и окончания.
- Сделать выбор времени с шагом 15 минут.
- Проверять, что время окончания позже времени начала.
- Реализовать предупреждение о конфликте времени с выбором: сохранить несмотря на конфликт или вернуться к редактированию.
- При выборе категории "Дни рождения" автоматически делать запись ежегодно повторяющейся, без времени, с отдельной иконкой и подписью "ДР".
- Реализовать фильтры по нескольким категориям одновременно и кнопку показа всех категорий.
- Не сохранять выбранные фильтры после перезагрузки.

### Готовый результат

Пользователь может создавать обычные задачи, задачи без времени, задачи с диапазоном времени и дни рождения. Данные проходят валидацию, сохраняются в `localStorage` и отображаются с учетом фильтров.

### Автопроверка

- Проверить, что задача без названия, категории или даты не сохраняется.
- Проверить, что задача с временем без начала или окончания не сохраняется.
- Проверить, что задача с окончанием раньше начала не сохраняется.
- Проверить, что доступные значения времени идут с шагом 15 минут.
- Проверить, что конфликт времени показывает предупреждение.
- Проверить, что после подтверждения конфликта задача сохраняется.
- Проверить, что день рождения получает ежегодное повторение и отображается с цветом, иконкой и подписью "ДР".
- Проверить, что фильтры могут включать несколько категорий.
- Проверить, что после перезагрузки фильтры сбрасываются.
- Проверить, что созданные задачи остаются после перезагрузки.

### Ручная проверка

- Оценить, удобно ли заполнять форму, которая раскрывается на основной панели.
- Оценить, понятны ли сообщения об ошибках и предупреждение о конфликте.
- Оценить, достаточно ли заметны дни рождения.

### Что сделано по факту

- Кнопка `+ Добавить новую задачу` раскрывает форму на основной панели.
- Форма сохраняет задачи в `localStorage` по ключу `dayPlanAppData`.
- Реализованы поля: название, категория, дата, "Без времени", время начала, время окончания, приоритет, повторение, заметка.
- Добавлен выбор дней недели для повтора `По выбранным дням недели`.
- Включены обязательные поля: название, категория и дата.
- Для задач с временем включена проверка времени начала, времени окончания и порядка времени.
- Добавлено предупреждение о конфликте времени с выбором: сохранить всё равно или вернуться к редактированию.
- Категория `Дни рождения` автоматически сохраняется как задача без времени с ежегодным повтором и подписью `ДР`.
- Реализованы фильтры по нескольким категориям одновременно.
- Кнопка `Все` возвращает отображение всех категорий.
- Выбранные фильтры не сохраняются после перезагрузки страницы.
- После сохранения задачи календарь переходит на дату этой задачи, чтобы пользователь сразу видел добавленную запись.

### Добавления сверх плана

- Добавлено модальное окно конфликта времени внутри страницы.
- Добавлены сообщения ошибок прямо в форме.
- Добавлена логика скрытия времени для задач без времени и дней рождения.
- Для дня рождения форма визуально выставляет `Без времени` и `Каждый год`, но итоговое правило также принудительно применяется при сохранении.
- После отмены форма очищается и скрывается.

### Автопроверка шага

- Проверен синтаксис `script.js` через встроенный Node.js.
- Проверено, что нет внешних подключений, импортов и библиотек.
- В Chrome проверено, что пустая задача не сохраняется и показывает ошибку про название.
- В Chrome проверено, что задача без времени сохраняется, появляется в верхнем блоке дня и остается после перезагрузки.
- В Chrome проверено, что повтор `По выбранным дням недели` без выбранных дней не сохраняется и показывает ошибку.
- В Chrome проверено, что задача с временем окончания раньше времени начала не сохраняется и показывает ошибку.
- В Chrome проверено, что конфликт времени открывает модальное предупреждение.
- В Chrome проверено, что при выборе `Вернуться к редактированию` конфликтная задача не сохраняется.
- В Chrome проверено, что при выборе `Сохранить всё равно` конфликтная задача сохраняется.
- В Chrome проверено, что день рождения сохраняется как `birthday`, `isAllDay: true`, `repeat.type: yearly` и отображается с подписью `ДР`.
- В Chrome проверено, что фильтр одной категории работает.
- В Chrome проверено, что можно выбрать несколько категорий.
- В Chrome проверено, что кнопка `Все` возвращает все категории.
- В Chrome проверено, что после перезагрузки фильтр сбрасывается.
- В Chrome проверено, что режимы неделя, месяц и год продолжают отрисовываться после изменений шага 3.

### Что осталось только для ручной проверки

- Оценить, удобно ли заполнять форму, которая раскрывается на основной панели.
- Оценить, понятны ли сообщения об ошибках и предупреждение о конфликте.
- Оценить, достаточно ли заметны дни рождения.

## Шаг 4. Редактирование, перенос и удаление задач — выполнен

### Что сделать

- Добавить на карточки задач кнопки "Редактировать", "Перенести", "Удалить".
- Реализовать редактирование обычной задачи через раскрывающуюся форму на основной панели.
- Реализовать перенос задачи через кнопку "Перенести" и форму переноса.
- При переносе разрешить выбрать новую дату, изменить время, убрать время или добавить время.
- Реализовать удаление через модальное окно внутри страницы.
- Для повторяющихся задач при редактировании уточнять: изменить только эту дату или всю серию.
- Для повторяющихся задач при переносе уточнять: перенести только эту дату или изменить всю серию.
- При изменении только одной даты использовать исключение в исходной серии и отдельную обычную задачу.
- При удалении повторяющейся задачи удалять всю серию с явным предупреждением.
- Не реализовывать drag-and-drop.

### Готовый результат

Пользователь может управлять задачами после создания: редактировать, переносить и удалять обычные и повторяющиеся задачи по правилам ТЗ.

### Автопроверка

- Проверить, что редактирование обычной задачи обновляет запись в `localStorage`.
- Проверить, что перенос обычной задачи меняет дату и при необходимости время.
- Проверить, что задачу с временем можно перенести и изменить ее время.
- Проверить, что задачу без времени можно перенести и добавить ей время.
- Проверить, что задачу с временем можно перенести и сделать задачей без времени.
- Проверить, что удаление обычной задачи требует подтверждения.
- Проверить, что отмена в модальном окне не удаляет задачу.
- Проверить, что удаление повторяющейся задачи удаляет всю серию.
- Проверить, что редактирование одного экземпляра повторяющейся задачи создает исключение и отдельную задачу.
- Проверить, что drag-and-drop отсутствует.

### Ручная проверка

- Оценить, понятны ли кнопки на карточках задач.
- Оценить, удобно ли переносить задачу через форму.
- Оценить, достаточно ли ясно модальное предупреждение при удалении серии.

### Что сделано по факту

- На карточки задач добавлены кнопки `Редактировать`, `Перенести`, `Удалить`.
- Реализовано редактирование обычной задачи через раскрывающуюся форму на основной панели.
- Реализован перенос обычной задачи через кнопку `Перенести` и ту же форму.
- При переносе можно изменить дату, изменить время, добавить время задаче без времени или убрать время.
- Реализовано удаление обычной задачи через модальное подтверждение.
- Отмена в модальном окне не удаляет задачу.
- Для повторяющихся задач при редактировании и переносе появляется уточнение: `Только эту дату` или `Всю серию`.
- При выборе `Только эту дату` исходная серия получает исключение в `excludedDates`, а измененная задача создается отдельной обычной записью с `sourceRecurringId`.
- При выборе `Всю серию` обновляется исходная повторяющаяся задача.
- При удалении повторяющейся задачи показывается предупреждение, что будет удалена вся серия.
- Drag-and-drop не реализован.
- Из недельного вида можно открыть дневной вид кликом по заголовку дня.

### Добавления сверх плана

- Стартовый экран изменен с `День` на `Неделя` по пользовательскому замечанию.
- Иконка категории `Дом` заменена на `🏠`.
- Для действий с повторяющимися задачами добавлено отдельное модальное окно выбора области действия.
- Для удаления добавлено отдельное модальное окно подтверждения.
- Форма задачи переиспользуется для создания, редактирования и переноса; заголовок формы меняется в зависимости от действия.
- После ручной проверки пользователь попросил заменить текстовые кнопки действий на компактные иконки. Принято: `✎` — редактировать, `⇄` — перенести, `×` — удалить, у каждой кнопки есть подсказка при наведении.
- После ручной проверки пользователь уточнил расположение иконок действий. Принято: иконки стоят внизу карточки, в один ряд, выровнены по левому краю содержимого; карандаш зелёный, перенос серый, удаление красное.
- После ручной проверки пользователь заметил, что форма редактирования открывается сверху страницы и не видно, сработал ли клик. Принято: редактирование и перенос открываются модальным окном поверх календаря, а создание через `+ Добавить новую задачу` остается раскрывающимся блоком на основной панели.
- После ручной проверки пользователь попросил добавить крестик вверху справа у формы задачи. Принято: в шапку формы добавлена круглая кнопка `×`, которая закрывает форму без сохранения.
- После ручной проверки пользователь попросил добавить проваливание в задачу двойным щелчком. Принято: двойной щелчок по карточке открывает редактирование, а для повторяющихся задач сначала открывает выбор `Только эту дату` или `Всю серию`.

### Автопроверка шага

- Проверен синтаксис `script.js` через встроенный Node.js.
- Проверено, что нет внешних подключений, импортов и библиотек.
- В Chrome проверено, что стартовый режим — `Неделя`.
- В Chrome проверено, что иконка дома отображается как `🏠`.
- В Chrome проверено, что клик по заголовку дня недели открывает дневной вид выбранной даты.
- В Chrome проверено, что обычная задача редактируется без изменения количества задач.
- В Chrome проверено, что обычная задача переносится на новую дату и новое время.
- В Chrome проверено, что удаление обычной задачи можно отменить.
- В Chrome проверено, что подтверждение удаления обычной задачи удаляет ее из `localStorage`.
- В Chrome проверено, что задача без времени переносится в задачу с временем.
- В Chrome проверено, что для повторяющейся задачи открывается модальное уточнение области действия.
- В Chrome проверено, что редактирование только одной даты повторяющейся задачи добавляет исключение в серию и создает отдельную обычную задачу.
- В Chrome проверено, что редактирование всей серии обновляет исходную повторяющуюся задачу.
- В Chrome проверено, что удаление повторяющейся задачи удаляет всю серию.
- В Chrome проверено, что действия на карточках отображаются компактными иконками, имеют `title` и `aria-label`, и клики по ним открывают редактирование, перенос и удаление.
- В Chrome проверено, что иконки действий находятся внизу карточки, выровнены слева, остаются компактными и имеют разные цвета для редактирования, переноса и удаления.
- В Chrome проверено, что `+ Добавить новую задачу` по-прежнему открывает обычную раскрывающуюся форму, без модального окна.
- В Chrome проверено, что `Редактировать` открывает форму как модальное окно поверх календаря, сразу заполняет данные задачи и переводит фокус в поле названия.
- В Chrome проверено, что `Перенести` открывает форму как модальное окно с заголовком `Перенести задачу`.
- В Chrome проверено, что `Отмена` закрывает модальное редактирование и снимает модальный режим формы.
- В Chrome проверено, что предупреждения о конфликтах времени будут показываться поверх модальной формы задачи.
- В Chrome проверено, что крестик вверху справа закрывает модальное редактирование без сохранения введенных изменений.
- В Chrome проверено, что крестик также закрывает обычную форму добавления задачи.
- В Chrome проверено, что двойной щелчок по обычной карточке задачи открывает модальное редактирование с заполненными данными.
- В Chrome проверено, что двойной щелчок по повторяющейся карточке сначала открывает модальное уточнение области действия.
- В Chrome проверено, что одиночный клик по карточке в недельном виде не проваливает в день и не мешает двойному щелчку по задаче.

### Что осталось только для ручной проверки

- Оценить, понятны ли кнопки `Редактировать`, `Перенести`, `Удалить` на карточках.
- Оценить, удобно ли переносить задачу через форму.
- Оценить, достаточно ли ясно модальное предупреждение при удалении серии.
- Оценить, удобно ли начинать работу с недельного вида и проваливаться в день.

## Шаг 5. Полная логика повторений и корректное отображение во всех режимах — выполнен

### Что сделать

- Реализовать вычисление повторяющихся задач без физического создания всех будущих экземпляров.
- Поддержать типы повторения: ежедневно, еженедельно, ежемесячно, ежегодно, по выбранным дням недели.
- Для повтора по дням недели разрешить выбор нескольких дней.
- Учитывать `excludedDates` при отображении повторяющихся задач.
- Корректно отображать повторения в режимах день, неделя, месяц и год.
- Корректно учитывать повторяющиеся задачи при проверке конфликтов времени.
- Корректно учитывать повторяющиеся задачи при фильтрации.

### Готовый результат

Повторяющиеся задачи работают во всех календарных режимах, не размножаются в хранилище и корректно реагируют на редактирование, перенос, удаление и фильтрацию.

### Автопроверка

- Проверить ежедневный повтор на нескольких датах.
- Проверить еженедельный повтор на той же неделе и следующих неделях.
- Проверить ежемесячный повтор в следующих месяцах.
- Проверить ежегодный повтор, включая дни рождения.
- Проверить повтор по нескольким выбранным дням недели.
- Проверить, что повторение не отображается до даты начала задачи.
- Проверить, что дата из `excludedDates` не отображается.
- Проверить, что повторяющиеся задачи видны в день, неделю, месяц и год.
- Проверить, что фильтры применяются к повторяющимся задачам.
- Проверить, что конфликт времени учитывает отображаемый экземпляр повторяющейся задачи.

### Ручная проверка

- Оценить, понятно ли в интерфейсе видно, что задача повторяющаяся.
- Оценить, не перегружает ли повторение карточки и календарные ячейки.

### Что сделано по факту

- Подтверждено, что повторяющиеся задачи вычисляются на лету для выбранного периода и не создаются физически в `localStorage` на каждую будущую дату.
- Подтверждена работа типов повторения: ежедневно, еженедельно, ежемесячно, ежегодно, по выбранным дням недели.
- Подтверждено, что для повтора по выбранным дням недели можно хранить и отображать несколько дней недели.
- Подтверждено, что `excludedDates` скрывает отдельные даты повторяющейся серии.
- Подтверждено, что повторяющиеся задачи отображаются в режимах день, неделя, месяц и год.
- Подтверждено, что фильтры категорий применяются к повторяющимся задачам.
- Подтверждено, что проверка конфликтов времени учитывает экземпляры повторяющихся задач.

### Добавления сверх плана

- Код приложения на этом шаге не менялся: полная логика повторений уже была добавлена в предыдущих шагах, а шаг 5 завершен как отдельная проверка и фиксация поведения.
- Перед браузерными проверками текущие данные из `localStorage` временно сохранялись в резервную копию и после тестов были восстановлены.

### Автопроверка шага

- Проверен синтаксис `script.js` через встроенный Node.js.
- Проверена чистая логика повторов: ежедневный повтор появляется на следующих датах и не появляется до даты старта.
- Проверена чистая логика еженедельного повтора: задача появляется в тот же день недели на следующей неделе и не появляется в другой день недели.
- Проверена чистая логика ежемесячного повтора: задача появляется в следующий месяц в тот же день месяца.
- Проверена чистая логика ежегодного повтора, включая сценарий дня рождения.
- Проверена чистая логика повтора по нескольким выбранным дням недели.
- Проверено, что дата из `excludedDates` не отображается.
- В браузере проверено, что повторяющиеся задачи видны в недельном режиме.
- В браузере проверено, что ежедневный повтор виден в дневном режиме на следующий день.
- В браузере проверено, что еженедельный повтор не виден в дневном режиме в неправильный день недели.
- В браузере проверено, что исключенная дата повтора по дням недели скрыта, а следующая подходящая дата отображается.
- В браузере проверено, что ежемесячный повтор виден в режиме месяца.
- В браузере проверено, что ежегодный день рождения виден в годовом режиме через сводку `ДР`.
- В браузере проверено, что повторяющиеся задачи имеют бейдж `Повтор`.
- В браузере проверено, что фильтр категории показывает повтор нужной категории и скрывает повторы других категорий.
- В браузере проверено, что после отображения в разных режимах и фильтрации количество исходных задач в `localStorage` не увеличивается.
- В браузере проверено, что при создании новой задачи на время, занятое повторяющейся задачей, открывается модальное предупреждение о конфликте.
- В браузере проверено отсутствие ошибок в консоли после восстановления исходных данных и перезагрузки страницы.

### Что осталось только для ручной проверки

- Оценить, понятно ли в интерфейсе видно, что задача повторяющаяся.
- Оценить, не перегружает ли повторение карточки и календарные ячейки.

## Шаг 6. Визуальная доводка, адаптивность и финальная приемка — выполнен

### Что сделать

- Довести визуальный стиль до уютного личного планировщика с нежной лиловой темой.
- Проверить, что категории различаются цветами.
- Проверить, что приоритет не спорит с цветом категории.
- Сделать состояния наведения и активные состояния кнопок.
- Настроить адаптивность: на ноутбуке форма задачи раскрывается на основной панели, на экранах меньше `1024px` страница не ломается.
- Проверить, что на телефоне страница не ломается.
- Провести финальную проверку по критериям готовности из `tz.md`.

### Готовый результат

Приложение соответствует ТЗ, работает без сервера и базы данных, сохраняет данные в браузере, удобно выглядит на ноутбуке и не ломается на телефоне.

### Автопроверка

- Проверить отсутствие ошибок в консоли.
- Проверить сохранение данных после перезагрузки.
- Проверить все критерии готовности из раздела 33 `tz.md`.
- Проверить ширины экрана: больше `1024px`, меньше `1024px`, телефонная ширина.
- Проверить, что кнопка добавления и раскрывающаяся форма задачи остаются удобными на нужном брейкпоинте.
- Проверить, что все кнопки открывают нужные действия.
- Проверить, что все модальные окна можно подтвердить и закрыть.
- Проверить, что проект не использует внешние библиотеки, сервер, базу данных, импорт/экспорт и авторизацию.

### Ручная проверка

- Оценить общий внешний вид: уютный ли планировщик, приятна ли лиловая тема.
- Оценить удобство работы на ноутбуке.
- Оценить, легко ли отличать работу, детей, дни рождения, дом, здоровье и личные задачи.
- Оценить, не ломается ли восприятие интерфейса на телефоне.

### Что сделано по факту

- Доведены состояния наведения и фокуса для кнопок, полей, карточек задач, дней месяца и карточек месяцев в годовом виде.
- Добавлены плавные, ненавязчивые переходы для интерактивных элементов.
- Карточки задач получили более заметное состояние наведения, не конфликтующее с цветами категорий.
- Модальные окна получили ограничение по высоте и внутреннюю прокрутку, чтобы на маленьких экранах не выходить за пределы окна браузера.
- Для мобильной ширины уточнены отступы и размеры модальной формы задачи и модальных подтверждений.
- Проведена финальная приемка по актуальному продукту: с учетом пользовательских решений про стартовый режим `Неделя`, отсутствие правой панели, модальное редактирование и сквозную навигацию `Год -> Месяц -> День`.

### Добавления сверх плана

- На финальном шаге добавлена небольшая визуальная полировка hover/focus-состояний и мобильных модальных окон.
- Данные пользователя перед финальной браузерной приемкой временно сохранялись в резервную копию, после тестов были восстановлены.
- После финальной приемки пользователь попросил выводить заметку в карточке задачи. Принято: если поле `Заметка` заполнено, оно показывается под названием обычным нежирным текстом; пустая заметка не выводится; автозаметка `ДР` у дней рождения не дублируется.

### Автопроверка шага

- Проверен синтаксис `script.js` через встроенный Node.js.
- Проверено, что приложение подключает только локальные `style.css` и `script.js`.
- Проверено, что в приложении нет внешних библиотек, серверных запросов, импортов, экспортов, базы данных и авторизации.
- Проверено, что файлы приложения остаются в рамках `index.html`, `style.css`, `script.js`.
- В браузере проверено отсутствие ошибок в консоли.
- В браузере проверено, что данные загружаются из `localStorage`.
- В браузере проверено, что после тестов пользовательские данные восстановлены, а временные тестовые задачи удалены.
- В браузере проверены режимы `День`, `Неделя`, `Месяц`, `Год`.
- В браузере проверено, что в недельном режиме есть 7 колонок.
- В браузере проверено, что месячный режим строит календарную сетку.
- В браузере проверено, что годовой режим показывает 12 месяцев.
- В браузере проверено создание формы через `+ Добавить новую задачу`.
- В браузере проверена валидация пустого названия.
- В браузере проверено предупреждение о конфликте времени.
- В браузере проверено модальное редактирование задачи.
- В браузере проверен перенос задачи через модальную форму.
- В браузере проверено уточнение области действия для повторяющейся задачи.
- В браузере проверено модальное подтверждение удаления и отмена удаления.
- В браузере проверена фильтрация по категории.
- В браузере проверена ширина ноутбука `1366px`: общего горизонтального переполнения нет.
- В браузере проверена ширина меньше `1024px`: верхняя панель перестраивается, форма добавления и модальное редактирование доступны.
- В браузере проверена мобильная эмуляция `390px`: страница не дает общего горизонтального переполнения, недельная сетка прокручивается внутри, модальная форма помещается по ширине.
- В браузере проверено, что заметка задачи выводится под названием только при наличии текста.
- В браузере проверено, что заметка отображается нежирным шрифтом.
- В браузере проверено, что у задачи без заметки пустая строка не появляется.
- В браузере проверено, что у дня рождения с автозаметкой `ДР` не появляется дубль под названием, а плашка `ДР` остается.

### Что осталось только для ручной проверки

- Оценить общий внешний вид: уютный ли планировщик, приятна ли лиловая тема.
- Оценить удобство работы на ноутбуке.
- Оценить, легко ли отличать работу, детей, дни рождения, дом, здоровье и личные задачи.
- Оценить, комфортно ли воспринимаются hover/focus-состояния и модальные окна на телефоне.
